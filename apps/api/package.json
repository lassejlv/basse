{
  "name": "@basse/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --hot src/index.ts",
    "check": "tsc --noEmit -p tsconfig.json",
    "auth:secret": "bun x @better-auth/cli@latest secret"
  },
  "dependencies": {
    "@basse/db": "workspace:*",
    "@basse/emails": "workspace:*",
    "@basse/shared": "workspace:*",
    "@opencoredev/email-sdk": "^0.6.5",
    "better-auth": "latest",
    "bullmq": "^5.79.2",
    "drizzle-orm": "latest",
    "hono": "latest",
    "ioredis": "5.10.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
